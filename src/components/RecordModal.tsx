import {useEffect,useState} from 'react';import Modal from './Modal';import type {ContactRecord,RecordType} from '../types/models';
export default function RecordModal({record,onClose,onSave}:{record?:ContactRecord;onClose:()=>void;onSave:(r:ContactRecord)=>void}){const [type,setType]=useState<RecordType>(record?.type||'person');const [name,setName]=useState(record?.name||'');const [description,setDescription]=useState(record?.description||'');const [email,setEmail]=useState(record?.email||'');const [location,setLocation]=useState(record?.location||'');useEffect(()=>{},[]);return <Modal title={record?'Editar registro':'Nuevo registro'} onClose={onClose}><form onSubmit={e=>{e.preventDefault();if(!name.trim())return;onSave({id:record?.id||crypto.randomUUID(),name:name.trim(),description:description.trim(),email:email.trim()||undefined,location:location.trim()||undefined,type})}}><div className="form-grid"><label>Tipo<select value={type} onChange={e=>setType(e.target.value as RecordType)}><option value="person">Persona</option><option value="organization">Organización</option><option value="project">Proyecto</option></select></label><label>Nombre *<input autoFocus required value={name} onChange={e=>setName(e.target.value)}/></label><label className="full">Descripción<textarea rows={4} value={description} onChange={e=>setDescription(e.target.value)}/></label>{type!=='project'&&<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label>}<label>Ubicación<input value={location} onChange={e=>setLocation(e.target.value)}/></label></div><div className="modal-actions"><button type="button" className="btn secondary" onClick={onClose}>Cancelar</button><button className="btn primary">Guardar</button></div></form></Modal>}
